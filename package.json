{
  "name": "chronos",
  "version": "1.0.0",
  "scripts": {
    "start-backend": "cd ./chronos-back && npm install && npm start",
    "start-frontend": "cd ./chronos-front && npm install && npm start",
    "start": "npm run start-backend & npm run start-frontend"
  },
  "dependencies": {
  }
}
